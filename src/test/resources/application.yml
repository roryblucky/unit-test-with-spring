spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:unit_tests
  sql:
    init:
      schema-locations:
        - classpath:sql/schema.sql
      data-locations:
        - classpath:sql/data.sql


